import styled from 'styled-components'

export const HeaderCustom = styled.header`
    margin-bottom: 100px;
`;

export const AncoraCustom = styled.a`
    display: flex;
    transition: .2s;

    &:hover {
        transform: translateX(8px);
    }
`;